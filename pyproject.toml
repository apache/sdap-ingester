[project]
name = "sdap-ingester"
version = "0.1.0"
description = "TBD"
readme = "README"
requires-python = ">=3.11"
dependencies = []

[tool.uv.sources]
collection_manager = { workspace = true }
common = { workspace = true }
config_operator = { workspace = true }
granule_ingester = { workspace = true }

[tool.setuptools.packages.find]
where = ["."]
include = [
  "common*",
  "config_operator*",
  "granule_ingester*",
  "collection_manager*",
]

[tool.uv.workspace]
members = ["collection_manager", "common", "config_operator", "granule_ingester"]

[tool.uv]
package = true